import {$t} from '@/resources/init.js';
export const config = {
  module: 'dashboard',
  menuType: {
    overview: $t('router.dashboard.dashboardmanage'),
    topvisit: $t('router.dashboard.dashboardlist')
  }
};
