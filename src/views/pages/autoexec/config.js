import {$t} from '@/resources/init.js';
export const config = { 
  module: 'autoexec', 
  menuType: { 
    detail: $t('router.autoexec.detail'), 
    job: $t('router.autoexec.job'), 
    quickService: $t('router.autoexec.quickService'), 
    tool: $t('router.autoexec.tool'), 
    config: $t('router.autoexec.config') 
  } 
};
