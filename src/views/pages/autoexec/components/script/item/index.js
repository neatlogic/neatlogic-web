import common from './common'; //默认组件
import ConditionTool from './condition-tool'; //if block工具(native/IF-Block)
import LoopBlock from './loop-block'; //loop block工具(native/LOOP-Block)
export default {
  common,
  ConditionTool,
  LoopBlock
};
